@model IEnumerable<CoffeeApp.Models.CoffeeStore>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.StoreName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Location)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.IsOpen)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.StoreRating)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.StoreName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Location)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.IsOpen)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.StoreRating)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Eircode }) |
            @Html.ActionLink("Details", "Details", new { id=item.Eircode }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Eircode })
        </td>
    </tr>
}

</table>
